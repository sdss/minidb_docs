# minidb_docs

This repository contains table descriptions for the contents of `minidb`. To add descriptions to the tables, add text under the `Summary` section or after each column (after the ` - `). You can use the GitHub editor if you want.
